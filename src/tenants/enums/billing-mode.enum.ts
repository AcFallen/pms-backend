export enum BillingMode {
  FIXED_PRICE = 'fixed_price', // Precio fijo por habitación (no importa las horas, se cobra el precio de la habitación)
  MINIMUM_PRICE = 'minimum_price', // Precio mínimo (el recepcionista cobra según criterio, basándose en el precio mínimo)
}
